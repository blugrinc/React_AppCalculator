 // "rafce" - mi permette di creare velocemente le "funzioni base dell js. (Snipppets extensions)
import React from 'react'
 
 const Structure = ({children}) => {
   return (
     <div className='structure'>{children}</div>
   )
 }
 
 export default Structure